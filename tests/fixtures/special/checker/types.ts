export type Checked = boolean;

export type List = 'foo' | 'bar' | 'baz';

export type Status = string;

export interface Location {
  lat?: number;
  long: number;
}

export type LocationAlias = {
  lat?: number;
  long: number;
};
